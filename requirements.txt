yt-dlp>=2024.1.0
fastapi>=0.115.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
lyricsgenius>=3.0.0

# Stage 3 (forced alignment) — install separately when needed:
# aeneas

# Stage 4 (vocal separation + ASR) — install separately when needed:
# demucs
# deepgram-sdk>=3.0.0

# Stage 5 (translation) — install separately when needed:
# deepl>=1.18.0
